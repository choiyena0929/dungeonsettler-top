---
title: "网站前端系统执行版"
type: template
status: active
created: 2026-08-28
project: "AI Web"
tags: [前端, 设计系统, 网站模板, 游戏站]
---
# Dungeon Settlers Field Guide 前端系统

## 使用说明

复制这个文件到新站源码根目录，命名为 `前端系统.md`；同时复制 `设计合同模板.md`，填写成站点自己的 `设计合同.md`。

这个文件负责告诉 Codex 如何做前端。游戏事实、关键词、来源、版本和公开文案仍然放在项目自己的研究和内容文件里。

## 素材闭环（新站、首页重做、视觉系统变更必读）

本站必须复制 research/素材清单.json 和 scripts/检查素材使用.mjs。素材清单是前端的输入合同，不是备忘录：

- 每个首页至少登记两个不同角色的必需视觉锚点；其中至少一个能直接识别游戏身份，另一个连接具体玩法或工具。
- 每条资产都要有已确认的权利依据、本地路径、渲染路由、实现文件、裁切规则和 alt。
- 设计合同、视觉基准包和代码都使用同一个资产 ID。缺其中一处，不能进入视觉验收。
- CSS、通用图标和渐变只负责布局、层级与状态，不承担游戏身份。核心视觉必须来自已登记的官方游戏素材；禁止用原创 SVG、AI 生图或抽象占位图补位。
- 在正式站点把素材清单设为 ready，并运行 npm run check:assets；模板目录保留 template。

## 本站基本信息

| 项目 | 填写内容 |
|---|---|
| 站点名称 | Dungeon Settlers Field Guide |
| 站点类型 | 攻略优先 |
| 目标用户 | 刚进入 Early Access、第一次准备 settlement 或 dungeon expedition 的玩家 |
| 首要动作 | 从 Beginner Guide 开始，选择一个具体资源或研究问题 |
| 当前代表性页面 | `/guides/beginner-guide` |
| 当前视觉合同 | `设计合同.md` |
| 当前视觉基准包 | `research/visual-benchmark.md` |

## 技术底线

- 保留现有 React 19、Tailwind 4、Vinext/Next 和发布流程；视觉改造不顺手迁移框架。
- 文章和静态事实尽量服务端渲染。搜索、筛选、拖拽、面板和表单才使用客户端状态。
- 新增组件优先复制源码到项目自己的组件目录，审阅后再改造；不要引入黑盒 UI 包来锁定全站样式。
- 保留路由、内容数据、SEO、分析和核心工具行为。前端改造不能顺手改变业务边界。

## 视觉底座

`设计合同.md` 必须填写真实值。CSS 只引用语义 Token，不在组件里到处写孤立颜色。

| 语义 Token | 用途 |
|---|---|
| `--bg` / `--surface` / `--surface-raised` | 页面、卡片和悬浮层 |
| `--text` / `--text-muted` / `--text-on-accent` | 标题、正文、辅助信息和强调按钮文字 |
| `--line` / `--line-strong` | 普通边界、焦点前的结构边界 |
| `--accent` / `--accent-strong` | 主动作、当前状态和少量视觉记忆点 |
| `--success` / `--warning` / `--danger` | 结果、提醒、错误和风险状态 |
| `--focus` | 键盘焦点，不能只靠颜色区别 |

默认节奏：间距使用 4、8、12、16、24、32、48、64；主要触控目标至少 44×44 像素；正文控制在约 65—78 字符宽；标题使用 `text-balance`，长段落使用 `text-pretty`，数值表格使用 `tabular-nums`。

## 页面合同

每个新页面或大改页面先填这张表：

| 字段 | 本页内容 |
|---|---|
| 用户任务 | {{用户想完成的事情}} |
| 首要动作 | {{唯一主动作}} |
| 允许使用的事实 | {{来源或内容数据}} |
| 视觉重点 | {{游戏身份区域}} / {{操作效率区域}} |
| 移动端优先级 | {{移动端首先保留什么}} |
| 通过条件 | {{如何知道页面真的可用}} |

同步填写 `research/page-contracts.json`。每页必须有 `primaryAction`、至少两个不重复的 `requiredSections`、可回查来源清单的 `sourceRefs`、整数 `minWords` 和 `minInternalLinks`；这些字段会被 S2、research-pack 和 On-page 复验共同检查。页面合同不能只写一句“回答核心问题”就进入实现。

页面不得只由标题、渐变和同质卡片组成。首屏必须出现真实内容、明确动作或真实工具入口。

## 资产绑定

每个代表性页面在开始编码前，追加这张表。不要把资产选择留给实现时临时发挥。

| 页面区域 | 资产 ID | 本地路径或组件 | 路由 | 视觉职责 | 截图证据 |
|---|---|---|---|---|---|
| 首屏 | `dungeon-settlers-hero` | `public/game/official/hero.jpg` | / | 用官方聚落实机画面直接建立 Dungeon Settlers 身份 | `artifacts/quality/warm-game-assets-final-r9-desktop.png` / `warm-game-assets-final-r9-mobile.png` |
| 核心动作 | `dungeon-settlers-research-loop` | `public/game/official/settlement.jpg`、`public/game/official/dungeon.jpg`；`app/components/game-loop-diagram.tsx` | / | 用官方营地与地下城实机画面连接 settlement → research → expedition 阅读路径 | `artifacts/quality/warm-game-assets-final-r9-desktop.png` / `warm-game-assets-final-r9-mobile.png` |
| 顶部品牌 | `dungeon-settlers-capsule` | `public/game/official/capsule.jpg` | 全站 | 用 Steam 官方 capsule 替换 DS 字母占位 | `artifacts/quality/warm-game-assets-final-r9-desktop-viewport.png` |
| 官方视频 | `dungeon-settlers-youtube-trailer` | `public/game/official/youtube-trailer.jpg` | / | 链接 CanOpener 官方 Gameplay Trailer | `artifacts/quality/warm-game-assets-final-r9-desktop.png` |

背景纹理不可以作为首屏唯一资产。不同角色的资产要承担不同任务，不能把同一张图裁成两块后重复登记。

## 组件选型顺序

1. 先用 HTML、现有 CSS 和语义 Token 解决结构、层级、状态和响应式。
2. 基础组件优先参考并复制 [shadcn/ui](https://ui.shadcn.com/)：按钮、输入、卡片、对话框、Tabs、Tooltip、Sheet、Command。
3. 需要 Data Grid、Filters、Kanban、Timeline、Sortable 或复杂步骤时，再参考 [ReUI](https://reui.io/components/)，只复制当前页面真正需要的组合。
4. 功能稳定后，用 [Transitions.dev](https://transitions.dev/) 或 [beUI](https://beui.dev/) 添加一两个状态过渡。
5. 只有需要站点记忆点时才参考 [Rare UI](https://www.rareui.com/)。一个站点不超过一个大记忆点。
6. [Beautiful UI](https://www.beautifului.dev/) 只用于未来的 AI 助手、检索来源、工具调用、推荐或审批状态。

同一页面保持一套基础组件体系。当前站点默认选择 shadcn 风格；只有从零开始且决定采用 Base UI 时，才评估 [Coss UI](https://coss.com/ui)，不要把两套原语随意混用。

## 动效规则

- 动效必须解释状态、空间关系或结果变化；装饰性动效优先删除。
- 高频搜索、键盘输入、连续拖拽和快速翻页不做拖沓的入场动画。
- 普通过渡控制在约 150—250ms；需要更长时间时，必须说明原因。
- 筛选结果、面板展开、Tab 指示、成功、错误和数字变化可以使用有限动效。
- 必须支持 `prefers-reduced-motion`；关闭动效后信息顺序和可操作性不变。
- 参考 [You Don't Need Animations](https://emilkowal.ski/ui/you-dont-need-animations) 做删减判断。

## 新建页面流程

1. 读取本站 `AGENTS.md`、`PROJECT_LOCK.md`、`RELEASE_POLICY.md`（存在时）和当前阶段状态。
2. 读取本文件、`设计合同.md`、视觉基准与公开文案包。
3. 先写页面合同，确定唯一主动作和移动端优先级。
4. 做首个包含真实内容的代表性页面；不先做空壳组件库。
5. 完成功能后做视觉打磨：Token、层级、间距、状态、资产、动效和响应式。
6. 保存第一版桌面与移动截图，交给不沿用实现解释的独立审阅；问题写入 `artifacts/quality/quality-review.json`。
7. 定向修订 blocking/major 问题，重新生成第二轮截图并复验主要交互；两轮后仍失败则停在当前阶段，不批量扩页。
8. 运行项目自己的 `npm run verify` 和 `git diff --check`。部署、提交和推送按项目规则单独处理。

## 素材验收

在视觉验收前运行 npm run check:assets，并同时看桌面和移动截图：

- 素材清单中的首页必需资产均有来源、权利、文件、代码引用和设计合同映射。
- 遮住站名后仍能从画面、图标或玩法结构辨认游戏；换成另一款游戏名后不能仍然毫无违和。
- 真实游戏资产和机制锚点先于 CSS 装饰；不以“已使用游戏配色”作为通过理由。
- 对标截图中的密度、裁切或操作位置，在本页能指出具体的对应落点。

## 旧页面改造流程

- 先保留路由、数据、SEO 和核心动作。
- 先改 Token、导航、首屏、主要按钮、卡片层级和移动端布局。
- 一次只选择一个代表性页面作为实验面，不同时重写全站。
- 如果新视觉让用户更快理解或完成任务，再把稳定部分提取到共享模板。
- 不把所有站点改成同一种游戏皮肤；共享的是工程规则，不是每站的颜色和图案。

## 验收清单

- [ ] 首屏说明游戏身份、用户问题和下一步动作。
- [ ] 页面有真实内容，不是占位符、假数据或同质卡片墙。
- [ ] 主按钮、搜索、筛选、工具、详情链接和返回路径可用。
- [ ] 桌面和移动端都能完成首要动作，没有横向溢出。
- [ ] 主要控制有至少 44×44 像素触控区域，键盘焦点清晰。
- [ ] 图片有固定比例，长标题、长标签、空结果和错误状态不破坏布局。
- [ ] 动效有目的，支持减少动效，关闭后仍能完成任务。
- [ ] 前台没有内部关键词、分析过程、AI 工作记录和未核验事实。
- [ ] `npm run verify` 与 `git diff --check` 通过。
- [ ] 代表页已有两轮桌面/移动截图、独立审阅记录，且没有未解决的 blocking 或未解释的 major 问题。
