import type { Metadata } from "next";
import { libraryEntries } from "../content";
import { LibrarySearch } from "../components/library-search";
import { JsonLd, itemListStructuredData } from "../structured-data";
import { absoluteUrl } from "../site-config";

export const metadata: Metadata = { title: "Dungeon Settlers guide library", description: "Search and filter the current Dungeon Settlers guide library by the problem you need to solve next.", alternates: { canonical: absoluteUrl("/library") } };
export default function LibraryPage() { return <section className="page-shell"><JsonLd value={itemListStructuredData(libraryEntries)} /><a className="back-link" href="/">← Back to the Field Guide</a><p className="eyebrow">Find the next useful route</p><h1>Dungeon Settlers guide library</h1><p className="lead">Dungeon Settlers guide library search starts with the problem you need to solve next. Filter the current guides by problem or category, then open the route that matches your next settlement or expedition decision.</p><div className="page-actions"><a className="button primary" href="/tools/first-expedition-planner">Plan a first run <span aria-hidden="true">→</span></a></div><div className="hub-note"><h2>Choose the question before the page.</h2><p>The library collects the three guide routes that currently have a documented evidence path: a beginner route for the settlement and first expedition, a Clay route built from a dated secondary report, and a Research route that keeps the technology tree boundary visible.</p><p>Use the search field when you remember the problem but not the page title. Category and tags are descriptive navigation; they do not turn a pending mechanic into a confirmed rule. Every guide carries its own update date and source list so you can decide whether the page still fits the build you are playing.</p><p>If the question is still broad, start with the <a href="/tools/first-expedition-planner">first expedition planner</a>. If you have just seen an Early Access change, read the <a href="/updates">dated guide updates</a> before relying on a quantity, floor, or interface detail.</p><p>The search results are intentionally small. A result is a route into an existing page, with the guide&apos;s title, category, date, and evidence level kept together. There is no hidden catalogue behind the filter, and an empty result does not imply that a missing game mechanic has been researched. Use the return link to reset the question or go back to the field guide when the problem is wider than this release.</p><p>For a first visit, the Beginner route gives the broadest sequence. For a specific resource, the Clay route keeps the dated report separate from official system facts. For a system question, the Research route is the place to record what is known and what still needs a current-build check. The <a href="/updates">update record</a> explains when those boundaries were last reviewed.</p><p>Because this is a working launch slice, the library favors a short set of useful routes over a long list of unverified entries. That makes it easier to notice when a source changes and to keep the next action honest.</p></div><LibrarySearch entries={libraryEntries} /></section>; }
